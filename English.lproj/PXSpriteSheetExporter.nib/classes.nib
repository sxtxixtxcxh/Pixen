{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {export = id; updatePreview = id; }; 
            CLASS = PXSpriteSheetExporter; 
            LANGUAGE = ObjC; 
            OUTLETS = {animationsTable = NSTableView; sheetImageView = NSImageView; }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}